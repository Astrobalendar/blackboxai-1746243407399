{
  "dependencies": {
    "axios": "^1.9.0",
    "firebase": "^11.6.1",
    "html2pdf.js": "^0.10.3"
  },
  "devDependencies": {
    "@types/axios": "^0.9.36",
    "vite": "^6.3.5"
  },
  "scripts": {
    "build": "vite build",
    "deploy": "npm run build && firebase deploy"
  }
}
