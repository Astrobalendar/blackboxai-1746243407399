{
  "name": "astrobalendar-backend-functions",
  "description": "Firebase Cloud Functions for AstroBalendar astrology app",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "firebase emulators:start --only functions,firestore",
    "deploy": "firebase deploy --only functions",
    "dev": "nodemon --watch src/ --exec 'npm run serve'"
  },
  "engines": {
    "node": "18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "firebase-admin": "^13.3.0",
    "firebase-functions": "^6.3.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "typescript": "^5.3.3"
  }
}
